apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: chatapp-ingressroute
spec:
  entryPoints:
    - web
  routes:
    - match: Host(`localhost`)
      kind: Rule
      services:
        - kind: Service
          name: beta-chatapp-svc
          namespace: beta
          port: 80